# Production environment configuration for Docker
# Copy this file and adjust values for your VPS

# Logging level: error, warn, info, debug, trace
RUST_LOG=info

# Database path (inside container)
DATABASE_URL=/app/data/portfolio.db

# Server binding (0.0.0.0 for Docker)
HOST=0.0.0.0
PORT=8080

# Optional: Email settings (configure in admin panel)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
